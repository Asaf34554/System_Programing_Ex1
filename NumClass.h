#ifndef NUMCLASS_H
#define NUMCLASS_H

#include <stdio.h>


//will return if a number is Arnstrong number
int isArmstrong(int);

//will return if a number is a palindrome
int isPalindrome(int);

//will return if a number is prime
int isPrime(int);

//will return if a number is strong number
int isStrong(int);

#endif